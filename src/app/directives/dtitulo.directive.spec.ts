import { DtituloDirective } from './dtitulo.directive';

describe('DtituloDirective', () => {
  it('should create an instance', () => {
    const directive = new DtituloDirective();
    expect(directive).toBeTruthy();
  });
});
