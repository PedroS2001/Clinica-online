import { DresaltarDirective } from './dresaltar.directive';

describe('DresaltarDirective', () => {
  it('should create an instance', () => {
    const directive = new DresaltarDirective();
    expect(directive).toBeTruthy();
  });
});
