import { MarcoDirective } from './marco.directive';

describe('MarcoDirective', () => {
  it('should create an instance', () => {
    const directive = new MarcoDirective();
    expect(directive).toBeTruthy();
  });
});
