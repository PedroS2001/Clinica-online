import { PcapitalzePipe } from './pcapitalze.pipe';

describe('PcapitalzePipe', () => {
  it('create an instance', () => {
    const pipe = new PcapitalzePipe();
    expect(pipe).toBeTruthy();
  });
});
