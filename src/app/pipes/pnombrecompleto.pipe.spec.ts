import { PnombrecompletoPipe } from './pnombrecompleto.pipe';

describe('PnombrecompletoPipe', () => {
  it('create an instance', () => {
    const pipe = new PnombrecompletoPipe();
    expect(pipe).toBeTruthy();
  });
});
